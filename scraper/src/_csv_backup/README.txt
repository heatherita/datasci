This directory is an automated backup of the original `src/csv` package.
It was moved to avoid import name conflicts with the stdlib `csv` module.
If you need to restore files, move them back into `src/csv`.
