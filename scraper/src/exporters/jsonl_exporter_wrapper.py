"""Deprecated wrapper - JSONL exporter functionality moved to `src/jsonl/exporter.py`.

This stub exists to avoid breakage during the refactor. Use
`src/jsonl/exporter.py` instead.
"""
raise ImportError("jsonl exporter wrapper removed; use src/jsonl/exporter.py instead")
